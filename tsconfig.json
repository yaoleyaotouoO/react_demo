{
    // 编译器选项
    "compilerOptions": {
        // 将输出结构重定向到目录
        "outDir": "./dist/",
        // 生成相应的 .map 文件
        "sourceMap": true,
        // 使用隐含 any 类型提高表达式和声明的错误
        "noImplicitAny": true,
        // 指定生成哪个模块系统代码： "None"， "CommonJS"， "AMD"， "System"， "UMD"， "ES6"或 "ES2015"
        "module": "commonjs",
        // 指定ECMAScript的目标版本："ES3"(默认), "ES5", "ES6"/ "ES2015", "ES2016", "ES2017"或"ESNext"
        "target": "es5",
        // 在.tsx文件中支持JSX ："React"或"Preserve"
        "jsx": "react",
        // 为ES装饰器启用实验支持
        "experimentalDecorators": true,
        // 要包含在编译中的库文件列表
        "lib": [
            "es5",
            "es2015",
            "dom",
            "scripthost"
        ]
    },
    // 包含文件
    "include": [
        "src/**/*"
    ],
    // 排除文件
    "exclude": [
        "node_modules"
    ]
}